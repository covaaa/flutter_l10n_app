name: flutter_l10n_app
description: "Flutter example project with l10n."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.6.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
